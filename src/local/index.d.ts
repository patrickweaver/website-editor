/**
 * Enables local editing mode for the website editor
 * This function sets up event listeners and controls for editing various aspects of the page
 * including text, images, colors, layout, and metadata.
 */
export function localEditingMode(): void;
